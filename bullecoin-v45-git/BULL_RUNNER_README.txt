BULLE v6 — Bull Runner Beta

Playable local beta included.

IMPORTANT: the included leaderboard is stored only in each browser. Before paying official weekly prizes, add a persistent database, server-side score verification, anti-cheat checks, published eligibility rules and a verified payout workflow.

Install by replacing app/, components/, public/ and tsconfig.json, then run npm run build.
